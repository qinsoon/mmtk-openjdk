includes:
  - "../base.yml"

# Only a subset of plans are tested on aarch64 for now. Add more plans here (and their expected results
# in ci-expected-results.yml) as they get supported.
# Compressed oops are not supported on aarch64 yet, so every config uses `no_compressed_oops`.
configs:
  - "jdk21-master|dacapochopin_jdk21|ms|s|fail_on_oom|tph|preserve|no_compressed_oops|mmtk_gc-Immix"

# This will be expanded in CI when we run with the config. Keep a new line at the end.
benchmarks:
  dacapochopin:
